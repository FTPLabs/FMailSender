# FMailSender — Трекер задач и проблем

  > **Правило:** Проблема считается закрытой ТОЛЬКО если пользователь явно сказал «да, устранена».  
  > До этого статус остаётся 🔴 ОТКРЫТА или 🟡 В РАБОТЕ.

  ---

  ## 🔴 ОТКРЫТЫЕ ПРОБЛЕМЫ

  *(нет открытых)*

  ---

  ## ✅ ЗАКРЫТЫЕ ПРОБЛЕМЫ

  ### SND-001 — Рассылка: общие проблемы при отправке
  **Файлы:** `core/sender.py`, `core/models.py`
  **Описание:** Проблемы при отправке писем.  
  **Исправления:**
  1. `core/models.py` (v6.0.3) — добавлен `__post_init__` в SmtpAccount: создаются `_lock`, `_day_reset`, `_hour_reset`. Исправляет `AttributeError: 'SmtpAccount' object has no attribute '_lock'` при запуске рассылки.
  2. `core/sender.py` (v6.0.4) — `asyncio.get_event_loop()` заменён на `asyncio.get_running_loop()` внутри корутины `run_campaign()`. `get_event_loop()` deprecated в Python 3.10+ в контексте работающего loop.
  **Статус:** ✅ ЗАКРЫТА (v6.0.3 + v6.0.4)

  ### GUI-001 — Логотип в диалоге обновления (только v5 PyQt)
  **Описание:** В v5 PyQt-версии в окне обновления отображался SVG-иконка щита вместо логотипа.  
  **Решение:** Устаревшая проблема — v6 не использует PyQt6.  
  **Статус:** ✅ ЗАКРЫТА (архитектура v6)

  ### ACC-001 — Проверка аккаунтов без прокси (только v5 PyQt)
  **Описание:** В v5 при нажатии «Проверить» без прокси — ошибка.  
  **Решение:** v6 — `/api/accounts/test`, прокси не обязателен.  
  **Статус:** ✅ ЗАКРЫТА (архитектура v6)

  ### PRX-001 — Прокси сбрасываются при перезапуске (только v5 PyQt)
  **Решение:** v6 `core/storage.py` сохраняет прокси в `global_proxies.json`.  
  **Статус:** ✅ ЗАКРЫТА (v6 storage.py)

  ### PRX-002 — Импорт аккаунтов без глобального пула (только v5 PyQt)
  **Решение:** v6 UI (React) — управление через /api.  
  **Статус:** ✅ ЗАКРЫТА (архитектура v6)

  ---

  ## 📋 Инструкция для агента

  1. При начале каждой сессии — прочитать этот файл
  2. Все новые проблемы добавлять в раздел «ОТКРЫТЫЕ»
  3. Переводить в «ЗАКРЫТЫЕ» ТОЛЬКО по явному подтверждению пользователя
  4. Не удалять проблемы самостоятельно — только переносить
  5. **Архитектура v6**: Tauri v2 + Python FastAPI (`core/`) + React/Vite (`ui/`) — НЕТ PyQt6/Qt
  