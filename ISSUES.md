# FMailSender — Трекер задач и проблем

> **Правило:** Проблема считается закрытой ТОЛЬКО если пользователь явно сказал «да, устранена».
> До этого статус остаётся 🔴 ОТКРЫТА или 🟡 В РАБОТЕ.

---

## 🔴 ОТКРЫТЫЕ ПРОБЛЕМЫ

_Нет открытых проблем._

---

## ✅ ЗАКРЫТЫЕ ПРОБЛЕМЫ

### BOOT-2 — PORT_WAIT_SECS=90 вызывал ложный timeout (130 сек)
    **Файлы:** `src-tauri/src/main.rs`, `ui/src/components/StartupOverlay.tsx`
    **Решение v7.1.3:** PORT_WAIT_SECS 90→150, SPAWN_ALIVE_CHECK_S 6→10.
    **Статус:** ✅ ЗАКРЫТА (v7.1.3, 2026-07-06)

    ### VER-2 — FRONTEND_VERSION="7.0.2" вызывал бесконечный reload (КРИТИЧЕСКИЙ)
    **Файлы:** `ui/src/version.ts`
    **Решение v7.1.3:** Все версии синхронизированы на 7.1.3.
    **Статус:** ✅ ЗАКРЫТА (v7.1.3, 2026-07-06)

    ### BOOT-1 — Бесконечная загрузка при запуске
**Файлы:** `core/license.py`, `src-tauri/src/main.rs`, `ui/src/components/StartupOverlay.tsx`, `.github/workflows/release.yml`
**Описание:** Приложение показывало бесконечный экран загрузки и не запускалось.
Три взаимосвязанные причины:
1. **PyInstaller onefile** распаковывал всё в `%TEMP%\_MEI<random>/` при каждом запуске.
   Windows Defender сканировал каждый файл → 60-120 сек задержки.
   PORT_WAIT_SECS=90 истекал раньше, чем Python запускался.
2. **WMIC-команды** (`wmic csproduct get UUID`, `wmic cpu get ProcessorId`) устарели
   в Windows 10 21H1+ и удалены в ряде сборок Windows 11. Зависали и возвращали пустые результаты.
3. **Кнопка «Перезапустить ядро»** появлялась ТОЛЬКО при `coreStage === 'failed'`.
   Во время AV-сканирования стадия всегда `killed`/`spawning` — кнопка никогда не показывалась.
**Решение v6.9.2:**
- Переход с PyInstaller на **Nuitka** — компилирует Python в нативный C/PE-binary.
  Нет распаковки в TEMP, нет PyInstaller-сигнатур для AV, кэш в `%LOCALAPPDATA%`.
- **WMIC удалён** из `core/license.py`, заменён на `wmi` Python-пакет (уже в requirements.txt).
- **PORT_WAIT_SECS** увеличен с 90 с → 200 с.
- **SPAWN_MAX_RETRIES** уменьшен с 25 → 8 (меньше бесполезных повторов).
- **Кнопка «Перезапуск»** теперь появляется через 60 сек независимо от стадии.
- Добавлена подсказка «Windows Defender может сканировать файлы» после 30 сек.
**Статус:** ✅ ЗАКРЫТА (v6.9.2, 2026-07-02)

### PORT-1 — NSIS launcher создавал ярлык на рабочем столе при запуске
**Решение v6.9.0:** CI теперь шипит сырой Tauri exe (`fmail-sender.exe`) напрямую.
**Статус:** ✅ ЗАКРЫТА (v6.9.0, 2026-07-02)

### PATH-1 — ReplyMonitor.save_sent_id использовал хардкодный путь data/
**Решение v6.9.0:** Значение по умолчанию заменено на `None`, путь через `_get_data_dir()`.
**Статус:** ✅ ЗАКРЫТА (v6.9.0, 2026-07-02)

### SEC-1 — refresh_token хранится в открытом виде (storage.py)
**Решение v6.7.1:** Добавлено шифрование `refresh_token` через Fernet.
**Статус:** ✅ ЗАКРЫТА (v6.7.1, 2026-07-01)

### COMPAT-1 — load_campaign() падает с TypeError при несовместимости версий
**Решение v6.7.1:** Ключи фильтруются через `dataclasses.fields(CampaignConfig)`.
**Статус:** ✅ ЗАКРЫТА (v6.7.1, 2026-07-01)

### HANG-1 — IMAP reconnect в bounce.py зависает без таймаута
**Решение v6.7.1:** `_IMAP_TIMEOUT = 30` на уровне модуля, reconnect-путь передаёт `timeout`.
**Статус:** ✅ ЗАКРЫТА (v6.7.1, 2026-07-01)

### HANG-2 — IMAP в ReplyMonitor._check_inbox() без таймаута
**Решение v6.7.1:** Добавлен `timeout=30`.
**Статус:** ✅ ЗАКРЫТА (v6.7.1, 2026-07-01)

### REGEX-1 — Опечатка s* вместо \s* в _parse_reply (reply_monitor.py)
**Решение v6.7.1:** Исправлено на `\s*`.
**Статус:** ✅ ЗАКРЫТА (v6.7.1, 2026-07-01)

### SND-001 — AttributeError: 'SmtpAccount' object has no attribute '_lock'
**Решение v6.0.5:** Добавлен `__getattr__` safety-net.
**Статус:** ✅ ЗАКРЫТА (v6.0.5, 2026-06-30)

### SRV-001 — asyncio.get_event_loop() deprecated в check_proxies_endpoint
**Решение v6.0.5:** Заменено на `asyncio.get_running_loop()`.
**Статус:** ✅ ЗАКРЫТА (v6.0.5, 2026-06-30)

---

## 📋 Инструкция для агента

1. При начале каждой сессии — прочитать этот файл
2. Все новые проблемы добавлять в раздел «ОТКРЫТЫЕ»
3. Переводить в «ЗАКРЫТЫЕ» ТОЛЬКО по явному подтверждению пользователя
4. Не удалять проблемы самостоятельно — только переносить
5. **Архитектура v6**: Tauri v2 + Python FastAPI (core/) + React/Vite (ui/) — НЕТ PyQt6/Qt
6. **Сборка v6.9.2+**: Nuitka (не PyInstaller) — нативный PE-binary
