# FMailSender — Трекер задач и проблем

> **Правило:** Проблема считается закрытой ТОЛЬКО если пользователь явно сказал «да, устранена».
> До этого статус остаётся 🔴 ОТКРЫТА или 🟡 В РАБОТЕ.

---

## 🔴 ОТКРЫТЫЕ ПРОБЛЕМЫ

_Нет открытых проблем._

---

## ✅ ЗАКРЫТЫЕ ПРОБЛЕМЫ

### PORT-1 — NSIS launcher создавал ярлык на рабочем столе при запуске
**Файлы:** `portable.nsi`, `.github/workflows/release.yml`
**Описание:** Скачиваемый `FMailSender-vX.X.X.exe` был NSIS-обёрткой, которая при запуске:
1. Устанавливала настоящий `FMailSender.exe` в `%LOCALAPPDATA%\FMailSender\`
2. Создавала ярлык `FMailSender.lnk` на **рабочем столе**
3. Запускала приложение из %LOCALAPPDATA%, а не из скачанного файла
Нарушало требование: «скачанный файл — финальный .exe, никаких новых файлов на рабочем столе».
**Решение v6.9.0:** CI теперь шипит сырой Tauri exe (`fmail-sender.exe`) напрямую,
переименованный в `FMailSender-v{ver}.exe`. Python-ядро уже встроено через `include_bytes!()`.
**Статус:** ✅ ЗАКРЫТА (v6.9.0, 2026-07-02)

### PATH-1 — ReplyMonitor.save_sent_id использовал хардкодный путь data/
**Файлы:** `core/reply_monitor.py`
**Описание:** `path: Path = Path("data/sent_message_ids.json")` — относительный путь
от CWD. В замороженном PyInstaller exe CWD указывает на временную папку извлечения,
не на директорию данных. Message IDs записывались в неверное место (system TEMP).
**Решение v6.9.0:** Значение по умолчанию заменено на `None`. Внутри функции
путь вычисляется через `_get_data_dir()` из `core/storage.py` — тот же каталог %APPDATA%.
**Статус:** ✅ ЗАКРЫТА (v6.9.0, 2026-07-02)

### SEC-1 — refresh_token хранится в открытом виде (storage.py)
**Файлы:** `core/storage.py`
**Описание:** `save_accounts()` шифровал `password` и `access_token`, но `refresh_token`
записывался в `accounts.json` в открытом виде. OAuth2 refresh token — долгоживущий
(часто бессрочный) и позволяет получить новый access token без пароля.
**Решение v6.7.1:** Добавлено шифрование/дешифрование `refresh_token` через Fernet в
`save_accounts()` / `load_accounts()` — наравне с `access_token`.
**Статус:** ✅ ЗАКРЫТА (v6.7.1, 2026-07-01)

### COMPAT-1 — load_campaign() падает с TypeError при несовместимости версий (storage.py)
**Файлы:** `core/storage.py`
**Описание:** `CampaignConfig(**d)` выбрасывает `TypeError: unexpected keyword argument`
если `campaign.json` был сохранён другой версией (поле добавлено или удалено).
**Решение v6.7.1:** Ключи фильтруются через `dataclasses.fields(CampaignConfig)` перед
созданием объекта. Импортирован `import dataclasses`.
**Статус:** ✅ ЗАКРЫТА (v6.7.1, 2026-07-01)

### HANG-1 — IMAP reconnect в bounce.py зависает без таймаута
**Файлы:** `core/bounce.py`
**Описание:** При `IMAP4.abort` reconnect создавал соединение без `timeout` — поток
мог висеть бесконечно.
**Решение v6.7.1:** `_IMAP_TIMEOUT = 30` перенесён на уровень модуля; reconnect-путь
теперь передаёт `timeout=_IMAP_TIMEOUT`.
**Статус:** ✅ ЗАКРЫТА (v6.7.1, 2026-07-01)

### HANG-2 — IMAP в ReplyMonitor._check_inbox() без таймаута
**Файлы:** `core/reply_monitor.py`
**Описание:** `imaplib.IMAP4_SSL(self._host, self._port)` без `timeout` — фоновый
поток зависал при недоступном сервере.
**Решение v6.7.1:** Добавлен `timeout=30`.
**Статус:** ✅ ЗАКРЫТА (v6.7.1, 2026-07-01)

### REGEX-1 — Опечатка s* вместо \s* в _parse_reply (reply_monitor.py)
**Файлы:** `core/reply_monitor.py`
**Описание:** `r'"?([^"<]+)"?s*<?([^>]*)>?'` содержал `s*` (буква «s»)
вместо `\s*` (пробельные символы). Парсинг From-заголовка «Name <addr>» всегда
возвращал сырую строку вместо имени и адреса.
**Решение v6.7.1:** Исправлено на `\s*`.
**Статус:** ✅ ЗАКРЫТА (v6.7.1, 2026-07-01)

---

### SND-001 — AttributeError: 'SmtpAccount' object has no attribute '_lock'
**Решение v6.0.5:** Добавлен `__getattr__` safety-net.
**Статус:** ✅ ЗАКРЫТА (v6.0.5, 2026-06-30)

### SRV-001 — asyncio.get_event_loop() deprecated в check_proxies_endpoint
**Решение v6.0.5:** Заменено на `asyncio.get_running_loop()`.
**Статус:** ✅ ЗАКРЫТА (v6.0.5, 2026-06-30)

### GUI-001 — Логотип в диалоге обновления (только v5 PyQt)
**Статус:** ✅ ЗАКРЫТА (архитектура v6 не использует PyQt)

### ACC-001 — Проверка аккаунтов без прокси (только v5 PyQt)
**Статус:** ✅ ЗАКРЫТА (архитектура v6)

### PRX-001 — Прокси сбрасываются при перезапуске (только v5 PyQt)
**Статус:** ✅ ЗАКРЫТА (v6 storage.py)

### PRX-002 — Импорт аккаунтов без глобального пула (только v5 PyQt)
**Статус:** ✅ ЗАКРЫТА (архитектура v6)

### UI-001 — Страница «Входящие» недоступна (маршрут отсутствовал)
**Решение:** Добавлен маршрут `/inbox` и ссылка в боковую панель.
**Статус:** ✅ ЗАКРЫТА (2026-06-30)

### UI-002 — Захардкоженный 127.0.0.1:7531 в Layout.tsx (VPN-несовместимость)
**Решение:** Заменено на `getBaseUrl()`.
**Статус:** ✅ ЗАКРЫТА (2026-06-30)

---

## 📋 Инструкция для агента

1. При начале каждой сессии — прочитать этот файл
2. Все новые проблемы добавлять в раздел «ОТКРЫТЫЕ»
3. Переводить в «ЗАКРЫТЫЕ» ТОЛЬКО по явному подтверждению пользователя
4. Не удалять проблемы самостоятельно — только переносить
5. **Архитектура v6**: Tauri v2 + Python FastAPI (core/) + React/Vite (ui/) — НЕТ PyQt6/Qt
