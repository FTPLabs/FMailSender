name: Release FMailSender .exe

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Release tag'
        required: false
        default: 'v2.8.5'

jobs:
  build-release:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Runner OK on $RUNNER_OS"
