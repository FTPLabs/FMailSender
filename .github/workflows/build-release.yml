# DEPRECATED for v6+
  # This workflow is kept for reference only.
  # Use release.yml for all v6.x builds (Tauri + React + Python FastAPI).
  #
  # To trigger a build: go to Actions → "🚀 Build & Release" → Run workflow → enter version

  name: "Build & Release (Legacy v5)"

  on:
    workflow_dispatch:
      inputs:
        note:
          description: 'This workflow is for v5.x (PyInstaller only). For v6+ use release.yml'
          required: false
          default: 'Use Actions → "🚀 Build & Release" for v6'

  jobs:
    redirect:
      runs-on: ubuntu-latest
      steps:
        - name: "Notice"
          run: |
            echo "⚠️  This is the legacy v5 build workflow."
            echo "For FMailSender v6+ (Tauri + React + Python FastAPI),"
            echo "please use the '🚀 Build & Release' workflow (release.yml)."
            echo ""
            echo "Go to: Actions → 🚀 Build & Release → Run workflow"
  